# Build Feature

Workflow: read `AGENTS.md` and relevant docs with Repo Navigator → identify product/architecture/privacy impact → state plan and acceptance criteria → implement smallest slice → add relevant tests → run lint/typecheck/build → run `npm run verify` → independent Reviewer Agent.

Do not expand scope during cleanup. A failed gate or unresolved blocker means not done.
